{
  "name": "rapid-roster",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "sync:site-chrome": "node scripts/sync-site-chrome.js"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
